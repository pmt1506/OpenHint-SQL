# Normalize line endings to LF in the repo; check out as CRLF on Windows
* text=auto eol=lf

# Force CRLF for Windows-only batch/iss files
*.bat text eol=crlf
*.iss text eol=crlf

# Treat binaries correctly
*.dll binary
*.exe binary
*.png binary
*.ico binary
